# 2026-08-26

## 修复 GitHub Actions 自动打包失败（deprecated upload-artifact v2）

- 现象：三个平台（ubuntu/macos/windows）的 build run 全部秒拒，报 `actions/upload-artifact: v2` deprecated。
- 根因：远程仓库的 workflow（commit 7693df1）用了 `dAppServer/wails-build-action@v2`，该三方 action 内部调用已废弃的 `actions/upload-artifact@v2`，被 GitHub 自动判失败。仓库自身的 upload-artifact 其实已是 v4。
- 修复：本地已有重写版 workflow（commit 7d21806，改为直接 `wails build`，不再用 wails-build-action）但一直没推送。追加 commit 057a434（setup-go 1.23 → 1.27 对齐 go.mod 的 go 1.27.0），推送后新 run 32965139860 正常进入编译阶段。
- 教训：诊断 CI 失败时先 `git ls-remote` 对比远程 HEAD 与本地，本地修复可能只是没推上去。

## 2026-08-26 续 - Windows 通过，macOS/Linux 在 wails build 阶段失败

- run 32965139860 (057a434)：Windows 成功（4m22s），macOS 失败（1m25s）、Linux 失败（1m42s），Release 因不是 tag push + build 未全过被跳过
- Windows 产物（.exe / .msi）以 workflow artifact 形式存在于 run 页面，不是 GitHub Release
- macOS 端 `darwin/universal` 在 Apple Silicon runner 上需要 lipo/SDK，Linux 端 webkit2gtk-4.1 的 pkg-config/链接是高频踩坑点
- 日志 API 需认证，本地无 gh CLI 也无 token，无法自动取日志；下次遇到 CI 失败直接让用户贴错误最省事

## 仓库环境备忘

- 本仓库有后台自动同步工具，每隔几分钟以 "Sync: <时间戳>" 为 message 自动 commit + push；它会清掉 `.git/refs/remotes/`（origin/main 显示 [gone]），fetch 后 ref 又消失属正常现象，不影响 push。
- 本机无 gh CLI，查 GitHub Actions 状态用公开 API：`api.github.com/repos/wyzzgzhdcxy/wcj-go-text/actions/runs`。

## 2026-08-26 续2 - 改为只打包 Windows（用户决定）

- commit 6a55c29：应需求去掉 macOS/Linux 打包，workflow 简化为单一 `Build (Windows)` job
  - 删三平台矩阵，runs-on 固定 windows-latest；删 Build(macOS/Linux)、Install Linux packaging tools 步骤
  - Generate build time 去掉非 Windows 分支；artifact name 里 matrix.os → windows；上传/Release 的 path 只留 .exe/.msi
- 新 run 32966216321 已确认只有 Windows job。Release 仍由 v* tag 触发（job 本身跑 ubuntu-latest，仅下载产物+发 Release，与构建平台无关）

## 2026-08-26 续3 - 重写 README.md + 打 tag 发 Release

- commit 7c646e9：通读代码后重写 README，补全实际功能（旧 README 只写了文本处理，漏了视频/音乐下载、应用生成器）
- 项目真实功能四大块：文本处理、视频下载（yt-dlp/B站/M3U8）、音乐下载（网易云）、应用生成器（网页打包成桌面应用）
- 外部运行时依赖：yt-dlp、N_m3u8DL-RE（需在 PATH）；音乐缓存用 SQLite（modernc.org/sqlite）
- 仓库地址 github.com/wyzzgzhdcxy/wcj-go-text（旧 README 写成了 wangchaojun，已纠正）；无 LICENSE 文件
- 已打 v1.0.1 tag 触发 Release（后续用户确认是否保留此版本号）
